import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    container: {
        minHeight: "100vh",
    },
    form: {
        height: "400px",
        background: "#fff",
        padding: "4rem",
        borderRadius: "4px",
    }
});