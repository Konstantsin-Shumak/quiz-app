import {
    makeStyles
} from "@mui/styles";

export const useStyles = makeStyles({
    wrapper: {
        height: "100vh",
    },
    container: {
        display: "flex",
        height: "100%",
        width: "100%",
    },
    container__content_block: {
        height: "300px",
        padding: "2rem",
        color: "white",
        backgroundColor: "#3A6BEA",
        borderRadius: "10px",
    },
});