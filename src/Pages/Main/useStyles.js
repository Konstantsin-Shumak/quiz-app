import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    wrapper: {
        height: "100vh",
    },
    content_block: {
        height: "250px",
        padding: "2rem",
        backgroundColor: "#D3DFFF",
        borderRadius: "10px",
    },
});