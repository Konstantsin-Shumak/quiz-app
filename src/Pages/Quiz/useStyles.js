import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(theme => ({
    container: {
        minHeight: "100vh",
    },
    container__quiz_block: {
        padding: "2rem",
        backgroundColor: "#D3DFFF",
        borderRadius: "10px",

    },
    quiz_block: {
    }
}));