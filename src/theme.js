import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        secondary:{
            main: "#FBC343",
            dark: "#ffb91a",
        },
        background:{
            default: "#F7F0E3"
        }
    },
    typography:{
        fontSize: 16,
    }
});